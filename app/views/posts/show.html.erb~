<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @post.name %>
</p>

<p>
  <b>User:</b>
  <%= @post.user_id %>
</p>

<p>
  <b>Message:</b>
  <%= @post.message %>
</p>

   <% @comments.each do |comment| %>
   <td><div class="well sidebar-nav"><li> <%= comment.id %></li>
       <li><%= User.find(comment.user_id).email %> </li>
       <li><%= comment.updated_at %> </li>
       <li><%= comment.message %> </li></div>
   
   </td>
    <% end %>

<div class="well sidebar-nav"> 

 <%= form_for(@comment) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <%= f.hidden_field :post_id, :value => @post[:id].to_i %>
  <!-- <div class="field">
    <%= f.label :post_id %><br />
    <%= f.number_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>  -->
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message , :cols => "35", :rows => "3" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
 
 </div>
<!--<%= link_to 'Edit', edit_post_path(@post) %> -->|
<%= link_to 'Back', posts_path %>
